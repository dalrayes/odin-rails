<h1>All tags</h1>

<p>
	<% @tags.each do |tag| %>
	<li><%= link_to tag.name, tag_path(tag) %>
	<%= link_to "delete", tag_path(tag), data: {confirm: "Are you sure you want to delete?"},  method: :delete %>
</li>
	<% end %>
</p>